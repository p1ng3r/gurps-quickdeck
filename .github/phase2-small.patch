diff --git a/CHANGELOG.md b/CHANGELOG.md
index 0c9df7c..45e7981 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,3 +1,15 @@
+## [Unreleased] - v0.20.4-runtime-cleanup-phase2
+
+### Changed
+- Routes document hooks and direct UI refreshes through one overlay render scheduler that coalesces region requests and delays updates while the command desk is being dragged.
+- Splits the overlay into chrome, roster, center cockpit, and action-drawer templates so common drawer, carousel, favorite, settings, actor-item, and combat changes replace only affected regions.
+- Keeps the custom scrollbar manager alive across renders, removes its broad subtree mutation observer, and explicitly refreshes changed regions while preserving scroll and focus state.
+- Replaces per-roll focus-hook registration and five delayed focus attempts with one persistent Foundry V1/V2 focus controller and one delayed fallback.
+- Adds development timing summaries through `game.gurpsQuickDeckDebug.dumpRenderTimings()`.
+
+### Validation
+- Adds scheduler/focus smoke coverage, Handlebars block checks, template action/layout contract comparison, DOM structure checks, syntax checks, and whitespace checks.
+
 ## [Unreleased] - v0.20.4-runtime-cleanup-phase1
 
 ### Changed
diff --git a/docs/v0.20.4-runtime-cleanup-plan.md b/docs/v0.20.4-runtime-cleanup-plan.md
index e4374a0..e98f88b 100644
--- a/docs/v0.20.4-runtime-cleanup-plan.md
+++ b/docs/v0.20.4-runtime-cleanup-plan.md
@@ -26,11 +26,21 @@ Validation:
 
 ## Phase 2 — Render and observer efficiency
 
+Status: implemented on `codex/v0.20.4-runtime-cleanup-phase2`; manual Foundry validation pending.
+
 - Add a single render scheduler that coalesces direct UI refresh requests as well as document-hook refreshes.
 - Split the overlay into roster, cockpit, actions, and popup render regions while preserving the current Handlebars data/action contracts.
 - Keep the custom scrollbar manager alive for the overlay lifetime and refresh only affected hosts.
 - Replace per-roll focus hook/timer creation with one persistent native-window focus controller.
-- Add development-only timing counters for view-model construction, template rendering, listener activation, and scrollbar setup.
+- Add development timing counters for view-model construction, template rendering, DOM/listener work, and total render duration.
+
+Implementation notes:
+
+- The visible overlay is composed from four region templates while retaining the original `data-action` contract and direct-child layout structure.
+- Existing `render(false)` calls now enter the central scheduler; high-frequency drawer, carousel, favorites, settings, item, combat, and modifier-bucket paths request narrower regions.
+- Region replacement preserves drawer/carousel scroll offsets and restores focus to the matching action control when possible.
+- The scrollbar manager retains one window listener and `ResizeObserver`; region changes explicitly release and rescan affected hosts.
+- Native-window focus uses persistent `renderApplicationV1` and `renderApplicationV2` listeners rather than installing three listeners for every roll.
 
 Exit criteria:
 
diff --git a/docs/v0.20.4-runtime-cleanup-validation.md b/docs/v0.20.4-runtime-cleanup-validation.md
index 9733eee..84fbfc3 100644
--- a/docs/v0.20.4-runtime-cleanup-validation.md
+++ b/docs/v0.20.4-runtime-cleanup-validation.md
@@ -1,12 +1,18 @@
 # v0.20.4 Runtime Cleanup Validation
 
-Automated checks completed for the Phase 1 cleanup branch:
+Automated checks completed for the Phase 1 and Phase 2 cleanup branches:
 
 - `node --check scripts/main.js`
 - `node --check scripts/quickdeck-app.js`
 - Headless QuickDeck smoke test covering the empty Application host model, overlay view-model construction, and native pending-damage payload creation.
 - ZIP integrity test for the locally packaged Phase 1 module.
 - GitHub branch comparison confirming that temporary patch/workflow files were removed.
+- Phase 2 scheduler smoke test confirming center/right requests coalesce into one render and a full request supersedes partial requests.
+- Focus-controller smoke test confirming only two persistent Foundry render hooks are installed and no new hooks are created per focus lock.
+- Handlebars block-balance checks for the overlay shell and all four region templates.
+- Template contract comparison confirming all 89 existing `data-action` occurrences and the primary UI2 layout classes remain present exactly once.
+- Parsed DOM structure check confirming the chrome, left drawer, center cockpit, right drawer, edge tabs, and region markers compose correctly.
+- Static check confirming the broad subtree `MutationObserver` was removed and the five focus retry delays were reduced to one delayed fallback.
 
 Manual validation still required before merge:
 
@@ -16,3 +22,6 @@ Manual validation still required before merge:
 4. Confirm players do not receive or apply the GM pending-damage queue.
 5. Open the developer art tuner and confirm it loads on demand.
 6. Exercise roster, favorites, carousel, skills, spells, PDF references, and drawer collapse controls.
+7. Scroll the right action drawer, pin or unpin an item, and confirm the drawer keeps its scroll position and the clicked control regains focus.
+8. Generate multiple actor/item/combat changes close together and inspect `game.gurpsQuickDeckDebug.dumpRenderTimings()` to confirm they coalesce.
+9. Confirm native GURPS windows still rise above QuickDeck after repeated primary, secondary, attack, defense, and damage rolls.
diff --git a/scripts/main.js b/scripts/main.js
index 901b2e4..68094a7 100644
--- a/scripts/main.js
+++ b/scripts/main.js
@@ -36,23 +36,10 @@ function openQuickDeck() {
   return quickDeckApp;
 }
 
-const QUICKDECK_RENDER_DEBOUNCE_MS = 25;
-const QUICKDECK_DRAG_RENDER_RETRY_MS = 100;
-let pendingQuickDeckRender = null;
-function renderQuickDeckIfOpen(delay = QUICKDECK_RENDER_DEBOUNCE_MS) {
+const QUICKDECK_DOCUMENT_RENDER_DEBOUNCE_MS = 25;
+function renderQuickDeckIfOpen(regions = "all", delay = QUICKDECK_DOCUMENT_RENDER_DEBOUNCE_MS) {
   if (!quickDeckApp?.rendered || quickDeckApp?.isMinimized) return;
-  if (pendingQuickDeckRender) return;
-
-  pendingQuickDeckRender = setTimeout(() => {
-    pendingQuickDeckRender = null;
-    if (!quickDeckApp?.rendered || quickDeckApp?.isMinimized) return;
-    if (quickDeckApp.isOverlayDragging?.()) {
-      renderQuickDeckIfOpen(QUICKDECK_DRAG_RENDER_RETRY_MS);
-      return;
-    }
-    quickDeckApp.render(false, { focus: false });
-    quickDeckApp.scheduleNativeWindowFocusAfterRender?.();
-  }, delay);
+  quickDeckApp.requestOverlayRender?.(regions, { delay, reason: "document-hook" });
 }
 
 function actorAffectsQuickDeckView(actorId, options = {}) {
@@ -78,6 +65,11 @@ Hooks.once("ready", () => {
       }
       quickDeckApp.dumpActiveActorData();
     },
+    dumpRenderTimings: () => {
+      const timings = quickDeckApp?.getOverlayRenderTimingSummary?.() ?? null;
+      console.table(timings?.summary ?? {});
+      return timings;
+    },
   };
 });
 
@@ -277,36 +269,29 @@ Hooks.on("createItem", (item) => {
   const actorId = item?.parent?.id ?? item?.actor?.id ?? null;
   if (!quickDeckApp || !actorId) return;
   quickDeckApp.invalidateDerivedActorData(actorId);
-  if (actorAffectsQuickDeckView(actorId, { includeRoster: false })) renderQuickDeckIfOpen();
+  if (actorAffectsQuickDeckView(actorId, { includeRoster: false })) renderQuickDeckIfOpen(["center", "right"]);
 });
 
 Hooks.on("updateItem", (item) => {
   const actorId = item?.parent?.id ?? item?.actor?.id ?? null;
   if (!quickDeckApp || !actorId) return;
   quickDeckApp.invalidateDerivedActorData(actorId);
-  if (actorAffectsQuickDeckView(actorId, { includeRoster: false })) renderQuickDeckIfOpen();
+  if (actorAffectsQuickDeckView(actorId, { includeRoster: false })) renderQuickDeckIfOpen(["center", "right"]);
 });
 
 Hooks.on("deleteItem", (item) => {
   const actorId = item?.parent?.id ?? item?.actor?.id ?? null;
   if (!quickDeckApp || !actorId) return;
   quickDeckApp.invalidateDerivedActorData(actorId);
-  if (actorAffectsQuickDeckView(actorId, { includeRoster: false })) renderQuickDeckIfOpen();
+  if (actorAffectsQuickDeckView(actorId, { includeRoster: false })) renderQuickDeckIfOpen(["center", "right"]);
 });
 
 function refreshQuickDeckOnCombatChange() {
-  renderQuickDeckIfOpen();
+  renderQuickDeckIfOpen(["left", "center", "right"]);
 }
 
-let pendingModifierBucketRefresh = null;
 function refreshQuickDeckOnModifierBucketChange() {
-  if (!quickDeckApp?.rendered || quickDeckApp?.isMinimized) return;
-
-  if (pendingModifierBucketRefresh) clearTimeout(pendingModifierBucketRefresh);
-  pendingModifierBucketRefresh = setTimeout(() => {
-    pendingModifierBucketRefresh = null;
-    renderQuickDeckIfOpen();
-  }, 0);
+  renderQuickDeckIfOpen(["center", "right"], 0);
 }
 
 Hooks.on("renderModifierBucket", () => {
