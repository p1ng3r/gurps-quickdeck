# Draft PR Notes

## Summary

- Removes duplicate QuickDeck startup rendering and duplicate overlay view-model construction.
- Unifies pending damage around the native GURPS transfer payload used by the Apply Damage workflow.
- Uses Foundry's HTML chat-render hook and keeps damage capture GM-only.
- Lazy-loads the developer art tuner and removes stale UI build labels.

## Checks

- JavaScript syntax checks passed for `scripts/main.js` and `scripts/quickdeck-app.js`.
- Headless smoke coverage passed for host data, overlay data, and pending-damage payload creation.
- Local install ZIP passed `unzip -t` integrity validation.

## Manual QA before merge

Foundry 13 and Foundry 14 smoke testing remains required, especially native GURPS attack/damage dialogs, player-versus-GM damage behavior, drawers, carousel, favorites, references, and the on-demand art tuner.
