# v0.20.4 Runtime Cleanup Validation

Automated checks completed for the Phase 1 and Phase 2 cleanup branches:

- `node --check scripts/main.js`
- `node --check scripts/quickdeck-app.js`
- Headless QuickDeck smoke test covering the empty Application host model, overlay view-model construction, and native pending-damage payload creation.
- ZIP integrity test for the locally packaged Phase 1 module.
- GitHub branch comparison confirming that temporary patch/workflow files were removed.
- Phase 2 scheduler smoke test confirming center/right requests coalesce into one render and a full request supersedes partial requests.
- Focus-controller smoke test confirming only two persistent Foundry render hooks are installed and no new hooks are created per focus lock.
- Handlebars block-balance checks for the overlay shell and all four region templates.
- Template contract comparison confirming all 89 existing `data-action` occurrences and the primary UI2 layout classes remain present exactly once.
- Parsed DOM structure check confirming the chrome, left drawer, center cockpit, right drawer, edge tabs, and region markers compose correctly.
- Static check confirming the broad subtree `MutationObserver` was removed and the five focus retry delays were reduced to one delayed fallback.

Manual validation still required before merge:

1. Open, close, minimize, restore, and drag QuickDeck in Foundry 13 and Foundry 14.
2. Roll melee and ranged attacks through native GURPS handling.
3. Roll damage and confirm the GM-only pending-damage popup opens the native Apply Damage dialog for a targeted or selected defender.
4. Confirm players do not receive or apply the GM pending-damage queue.
5. Open the developer art tuner and confirm it loads on demand.
6. Exercise roster, favorites, carousel, skills, spells, PDF references, and drawer collapse controls.
7. Scroll the right action drawer, pin or unpin an item, and confirm the drawer keeps its scroll position and the clicked control regains focus.
8. Generate multiple actor/item/combat changes close together and inspect `game.gurpsQuickDeckDebug.dumpRenderTimings()` to confirm they coalesce.
9. Confirm native GURPS windows still rise above QuickDeck after repeated primary, secondary, attack, defense, and damage rolls.
